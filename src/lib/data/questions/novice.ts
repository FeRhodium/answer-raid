import type { Question } from '../types';

/**
 * 入门档:阅读题 —— **题干即教材**。
 * 5 题全部自带材料,不预设任何计算机先验知识;考的是「读规格、照着执行」的素养。
 */
export const noviceQuestions: Question[] = [
  {
    id: 'novice-1',
    tier: 'novice',
    tags: ['补码', '二进制'],
    prompt: '计算机用**固定位数**的二进制存整数,常见表示法叫**补码**:正数按普通二进制写;求一个正数的相反数,做法是 **按位取反,再 +1**。\n8 位下 `1` = `00000001`,`1` 取反得 `11111110`,再 +1 得 `11111111`,即 `-1`。\n按同样规则,8 位下 `-2` 的二进制是?',
    options: ['10000010', '11111101', '11111110', '10000001'],
    answer: 2,
    explain: '`2` = `00000010`,按位取反得 `11111101`,再 +1 得 `11111110`,这就是 `-2`。选 `11111101` 是**漏了最后那个 +1**;选 `10000010` 是拿最高位当符号位、其余照抄数值的朴素想法(它其实是 `-126`);选 `10000001` 则是把「取反」直接当成了 `-1`。',
  },
  {
    id: 'novice-2',
    tier: 'novice',
    tags: ['排序', '选择排序'],
    prompt: '**选择排序**:每一轮在「还没排好的部分」里找出最小的数,和这部分最左边的数交换位置,然后未排好部分缩短一格。\n例如 `[5, 2, 9, 4]`:第 1 轮找到 2 与 5 交换得 `[2, 5, 9, 4]`;第 2 轮在 `[5, 9, 4]` 里找到 4 与 5 交换得 `[2, 4, 9, 5]`;第 3 轮找到 5 与 9 交换得 `[2, 4, 5, 9]`,完成。\n注意它每轮「比较两数大小」的次数:第 1 轮 3 次、第 2 轮 2 次、第 3 轮 1 次 —— 对 n 个数,**比较次数恒为 n(n-1)/2,与数据长什么样无关**。\n那么在下面哪个数组上,它的**交换次数**最多?',
    code: `void selectionSort(int a[], int n) {
  for (int i = 0; i < n - 1; i++) {
    int minIdx = i;
    for (int j = i + 1; j < n; j++)
      if (a[j] < a[minIdx]) minIdx = j;   // 只记下标,不交换
    int t = a[i]; a[i] = a[minIdx]; a[minIdx] = t;  // 每轮交换一次
  }
}`,
    lang: 'cpp',
    options: ['[1, 2, 3, 4, 5, 6]', '[6, 5, 4, 3, 2, 1]', '[3, 1, 6, 2, 5, 4]', '都一样多'],
    answer: 1,
    explain: '交换次数取决于「当前最小值是否已经在正确位置」:完全递增时最小值每次都恰好在原位,一次都不用换;完全递减时每轮的最小值都在最右端,前 5 轮都得换,共 5 次。选「都一样多」的人是把材料里那句 **比较次数恒为 n(n-1)/2** 记串了 —— 比较确实恒为 15 次,但问的是**交换**。',
  },
  {
    id: 'novice-3',
    tier: 'novice',
    tags: ['Python', 'range'],
    prompt: 'Python 的 `range(a, b, c)` 生成一个整数序列:从 `a` 开始,每次加 `c`,直到**不小于** `b` 就停下(也就是 `b` 本身不包含在内);`c` 可以为负数。\n例如 `range(0, 5, 2)` 得到 0, 2, 4;`range(5, 0, -2)` 得到 5, 3, 1。\n那么 `list(range(1, 10, 3))` 的结果是?',
    code: `>>> list(range(0, 5, 2))
[0, 2, 4]
>>> list(range(5, 0, -2))
[5, 3, 1]
>>> list(range(1, 10, 3))`,
    lang: 'python',
    options: ['[1, 4, 7, 10]', '[1, 4, 7]', '[1, 3, 6, 9]', '[3, 6, 9]'],
    answer: 1,
    explain: '从 1 出发每次加 3:1、4、7、10,但 10 已经**不小于** 10,所以停,结果是 `[1, 4, 7]`。选 `[1, 4, 7, 10]` 是忘了右端不包含;选 `[1, 3, 6, 9]` 是把步长 3 当成了起始值、又把 1 当成公差;选 `[3, 6, 9]` 则是把 `a` 和 `c` 的位置记反了。',
  },
  {
    id: 'novice-4',
    tier: 'novice',
    tags: ['整除', '取余'],
    prompt: '在多数编程语言里,两个整数相除有两种结果:`//` 是**整除**(只留商),`%` 是**取余数**,两者满足\n**被除数 = 除数 × 商 + 余数**。\n例如 `17 // 5` 得 3、`17 % 5` 得 2,因为 17 = 5 × 3 + 2。\n那么 `1000 // 7` 和 `1000 % 7` 分别是多少?',
    code: `>>> 17 // 5, 17 % 5
(3, 2)
>>> 1000 // 7, 1000 % 7`,
    lang: 'python',
    options: ['143 和 1', '142 和 7', '143 和 -1', '142 和 6'],
    answer: 3,
    explain: '7 × 142 = 994,7 × 143 = 1001 已经超过 1000,所以商只能是 **142**;再由材料里的等式得余数 = 1000 - 994 = **6**。验算一遍:`7 × 142 + 6 = 1000`,正好对上。选 143 开头的两项是把除法「四舍五入」了;选 7 是把余数写成了除数 —— 余数**必须小于除数**。',
  },
  {
    id: 'novice-5',
    tier: 'novice',
    tags: ['循环', '累加'],
    prompt: '阅读下面这段程序:`s` 一开始是 0,然后让 `i` 依次取 1、2、3、…、10,每次执行 `s = s + i`。\n循环结束后 `s` 的值是多少?',
    code: `int s = 0;
for (int i = 1; i <= 10; i++) {
  s = s + i;
}`,
    lang: 'cpp',
    options: ['45', '55', '100', '10'],
    answer: 1,
    explain: '这是把 1 到 10 全部加起来,即 10 × 11 ÷ 2 = **55**。选 45 是把 1~9 相加(把 `i <= 10` 读成了 `i < 10`);选 100 是把它当成了 10 × 10;选 10 则是以为循环只执行最后一次,那是一种完全不同的赋值方式。',
  },
];
